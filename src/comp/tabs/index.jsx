import React from 'react'
import { useTabs } from '../../providers/tabs'

export default function Tabs() {
    const { tabs } = useTabs()

    return (
        <div className='tabs full-screen flex fixed-t-l'>

        </div>
    )
}